
@page "/RecruiterJobs"
@model WebPortal.Pages.RecruiterJobsModel
@using WebPortal.Models
@{
    ViewData["Title"] = "Trung Tâm Tuyển Dụng";
}

<div class="space-y-12 animate-in fade-in duration-700">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-end gap-6">
        <div>
            <span class="inline-flex items-center px-4 py-1.5 rounded-full text-[10px] font-black uppercase tracking-widest bg-indigo-50 border border-indigo-100 text-indigo-700 shadow-sm">Trình Quản Lý Tuyển Dụng</span>
            <h2 class="text-5xl font-black text-slate-800 tracking-tighter uppercase mt-2">Trung Tâm Tuyển Dụng</h2>
            <p class="text-slate-500 text-sm font-medium mt-1">Quản lý bài đăng và theo dõi hiệu suất tuyển dụng theo thời gian thực.</p>
        </div>
        <a href="/PostJob" class="px-10 h-14 bg-indigo-600 text-white shadow-xl shadow-indigo-100 rounded-2xl font-black uppercase tracking-widest text-[11px] hover:bg-indigo-700 transition inline-flex items-center justify-center">
            <i class="fas fa-plus-circle mr-2"></i> Tạo Bài Đăng Mới
        </a>
    </div>

    <!-- Stats -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
        <div class="p-8 bg-white border border-slate-200 rounded-3xl shadow-sm group hover:scale-[1.02] cursor-pointer transition-all duration-300">
            <div class="w-14 h-14 bg-indigo-600 rounded-[1.5rem] flex items-center justify-center text-white mb-6 shadow-xl shadow-indigo-100 group-hover:rotate-12 transition-transform"><i class="fas fa-eye text-xl"></i></div>
            <p class="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-2">Tổng Lượt Xem</p>
            <div class="flex items-baseline justify-between">
                <h3 class="text-3xl font-black text-slate-900 tracking-tighter">@Model.Stats.TotalViews.ToString("N0")</h3>
                <span class="text-[10px] font-black text-indigo-600 bg-indigo-50 px-2 py-1 rounded-lg border border-indigo-100">Khả Dụng</span>
            </div>
        </div>
        <div class="p-8 bg-white border border-slate-200 rounded-3xl shadow-sm group hover:scale-[1.02] cursor-pointer transition-all duration-300">
            <div class="w-14 h-14 bg-indigo-500 rounded-[1.5rem] flex items-center justify-center text-white mb-6 shadow-xl shadow-indigo-100 group-hover:rotate-12 transition-transform"><i class="fas fa-bookmark text-xl"></i></div>
            <p class="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-2">Được Lưu Lại</p>
            <div class="flex items-baseline justify-between">
                <h3 class="text-3xl font-black text-slate-900 tracking-tighter">@Model.Stats.TotalSaves.ToString("N0")</h3>
                <span class="text-[10px] font-black text-green-600 bg-green-50 px-2 py-1 rounded-lg border border-green-100">Quan Tâm</span>
            </div>
        </div>
        <div class="p-8 bg-white border border-slate-200 rounded-3xl shadow-sm group hover:scale-[1.02] cursor-pointer transition-all duration-300">
            <div class="w-14 h-14 bg-amber-500 rounded-[1.5rem] flex items-center justify-center text-white mb-6 shadow-xl shadow-amber-100 group-hover:rotate-12 transition-transform"><i class="fas fa-paper-plane text-xl"></i></div>
            <p class="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-2">Tổng Ứng Viên</p>
            <div class="flex items-baseline justify-between">
                <h3 class="text-3xl font-black text-slate-900 tracking-tighter">@Model.Stats.TotalApplications.ToString("N0")</h3>
                <span class="text-[10px] font-black text-amber-600 bg-amber-50 px-2 py-1 rounded-lg border border-amber-100">Tiềm Năng</span>
            </div>
        </div>
        <div class="p-8 bg-white border border-slate-200 rounded-3xl shadow-sm group hover:scale-[1.02] cursor-pointer transition-all duration-300">
            <div class="w-14 h-14 bg-slate-900 rounded-[1.5rem] flex items-center justify-center text-white mb-6 shadow-xl shadow-slate-200 group-hover:rotate-12 transition-transform"><i class="fas fa-chart-line text-xl"></i></div>
            <p class="text-[10px] font-black uppercase tracking-[0.25em] text-slate-400 mb-2">Tỷ Lệ Chuyển Đổi</p>
            <div class="flex items-baseline justify-between">
                <h3 class="text-3xl font-black text-slate-900 tracking-tighter">@(Model.Stats.TotalSaves > 0 ? (Model.Stats.TotalApplications * 100.0 / Model.Stats.TotalSaves).ToString("F1") + "%" : "0%")</h3>
                <span class="text-[10px] font-black text-slate-600 bg-slate-50 px-2 py-1 rounded-lg border border-slate-100">Chuyển Đổi</span>
            </div>
        </div>
    </div>

    <!-- Jobs Table -->
    <div class="overflow-hidden border border-slate-200/60 shadow-xl shadow-slate-200/30 bg-white rounded-3xl">
        <div class="overflow-x-auto">
            <table class="w-full text-left">
                <thead class="bg-slate-50/80 border-b border-slate-100">
                    <tr>
                        <th class="px-8 py-6 text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Thông Tin Công Việc</th>
                        <th class="px-6 py-6 text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Trạng Thái</th>
                        <th class="px-6 py-6 text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Lý Do Bị Chặn</th>
                        <th class="px-6 py-6 text-[10px] font-black text-slate-400 uppercase tracking-[0.2em] text-center">Ứng Viên</th>
                        <th class="px-8 py-6 text-[10px] font-black text-slate-400 uppercase tracking-[0.2em] text-right">Thao Tác</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-50">
                    @if (Model.Jobs.Count == 0)
                    {
                        <tr>
                            <td colspan="5" class="px-8 py-12 text-center text-slate-500">
                                Chưa có công việc nào. Vui lòng tạo bài đăng mới để bắt đầu.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var job in Model.Jobs)
                        {
                            <tr class="hover:bg-indigo-50/20 transition-all group @(job.Status == JobStatus.BLOCKED ? "bg-red-50/10" : "")">
                                <td class="px-8 py-8">
                                    <p class="font-black text-slate-800 text-lg group-hover:text-indigo-600 transition-colors">@job.Title</p>
                                    <p class="text-[10px] text-slate-400 font-bold uppercase tracking-widest mt-1.5 flex items-center gap-2">
                                        <i class="fas fa-location-dot text-indigo-400"></i> @job.Location
                                    </p>
                                </td>
                                <td class="px-6 py-8">
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-bold @(job.Status == JobStatus.APPROVED ? "bg-green-100 text-green-800" : job.Status == JobStatus.CHECKING ? "bg-yellow-100 text-yellow-800" : job.Status == JobStatus.BLOCKED ? "bg-red-100 text-red-800" : job.Status == JobStatus.DRAFT ? "bg-indigo-50 text-indigo-400 border border-indigo-100" : "bg-slate-100 text-slate-800")">
                                        @(job.Status switch
                                        {
                                            JobStatus.APPROVED => "Đã duyệt",
                                            JobStatus.CHECKING => "Đang chờ duyệt",
                                            JobStatus.BLOCKED => "Đã chặn (AI)",
                                            JobStatus.HIDDEN => "Đã ẩn",
                                            JobStatus.DRAFT => "Bản nháp",
                                            _ => job.Status.ToString()
                                        })
                                    </span>
                                </td>
                                <td class="px-6 py-8 max-w-xs">
                                    @if (job.Status == JobStatus.BLOCKED)
                                    {
                                        <div class="flex flex-col gap-2">
                                            <p class="text-[10px] text-red-500 font-bold leading-tight">@job.ModerationNote</p>
                                            @if (job.IsAppealed)
                                            {
                                                <span class="text-[9px] font-black text-amber-600 uppercase italic flex items-center gap-1">
                                                    <i class="fas fa-clock"></i> Đang chờ xử lý khiếu nại
                                                </span>
                                            }
                                            else
                                            {
                                                <button onclick="openAppeal('@job.Id')" class="text-[9px] font-black text-indigo-600 uppercase hover:underline text-left">Gửi Khiếu nại</button>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <span class="text-[10px] text-slate-300 italic uppercase font-black">@job.ModerationNote</span>
                                    }
                                </td>
                                <td class="px-6 py-8 text-center">
                                    <span class="inline-flex items-center justify-center w-12 h-12 rounded-2xl bg-indigo-50 text-indigo-600 text-sm font-black border border-indigo-100/50 shadow-inner">
                                        @Model.Applications.Count(a => a.JobId == job.Id)
                                    </span>
                                </td>
                                <td class="px-8 py-8 text-right">
                                    <div class="flex justify-end items-center gap-3">
                                        <div class="flex items-center gap-1.5 p-1.5 bg-slate-50 rounded-2xl border border-slate-100 shadow-inner">
                                            <a href="/JobDetail?Id=@job.Id" title="Xem & Chỉnh sửa chi tiết" class="w-10 h-10 bg-white text-indigo-600 hover:bg-indigo-50 rounded-xl transition-all flex items-center justify-center border border-slate-100 shadow-sm">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-page="/Jobs/JobApplications" asp-route-Id="@job.Id" title="Xem Ứng Viên" class="w-10 h-10 bg-white text-green-600 hover:bg-green-50 rounded-xl transition-all flex items-center justify-center border border-slate-100 shadow-sm">
                                                <i class="fas fa-users text-sm"></i>
                                            </a>
                                            <a asp-page="/Recruiter/JobPipeline" asp-route-id="@job.Id" title="Quản Lý Quy Trình" class="w-10 h-10 bg-slate-900 text-white hover:bg-slate-800 rounded-xl transition-all flex items-center justify-center border border-slate-800 shadow-sm">
                                                <i class="fas fa-layer-group text-xs"></i>
                                            </a>
                                        </div>
                                        @if (job.Status != JobStatus.BLOCKED)
                                        {
                                            <form method="post" asp-page-handler="ToggleStatus" class="inline-flex m-0">
                                                <input type="hidden" name="jobId" value="@job.Id" />
                                                <button type="submit" title="@(job.Status == JobStatus.APPROVED ? "Ẩn Công Việc" : "Hiện Công Việc")" class="w-11 h-11 rounded-xl transition-all flex items-center justify-center border @(job.Status == JobStatus.APPROVED ? "bg-slate-50 text-slate-400 hover:text-amber-600 hover:bg-amber-50 border-slate-100" : "bg-green-50 text-green-600 border-green-100 hover:bg-green-100")">
                                                    <i class="fas @(job.Status == JobStatus.APPROVED ? "fa-eye-slash" : "fa-eye")"></i>
                                                </button>
                                            </form>
                                        }
                                        <form method="post" asp-page-handler="Delete" class="inline-flex m-0" onsubmit="return confirm('Bạn có chắc chắn muốn xóa công việc này không?')">
                                            <input type="hidden" name="jobId" value="@job.Id" />
                                            <button type="submit" title="Xóa Công Việc" class="w-11 h-11 rounded-xl transition-all flex items-center justify-center border bg-red-50 text-red-400 border-red-100 hover:bg-red-100 hover:text-red-600">
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>

    <partial name="_Pagination" model='(Model.CurrentPage, Model.TotalPages, "?CurrentPage={0}")' />
</div>

<!-- Appeal Modal -->
<div id="appealModal" class="fixed inset-0 z-50 flex items-center justify-center hidden">
    <div class="fixed inset-0 bg-slate-900/50 backdrop-blur-sm transition-opacity" onclick="closeAppeal()"></div>
    <div class="bg-white rounded-[2rem] shadow-2xl w-full max-w-md mx-4 p-8 relative z-10 animate-in zoom-in-95 duration-200">
        <h3 class="text-xl font-black text-slate-900 mb-6">Khiếu Nại Chặn AI</h3>
        <form method="post" asp-page-handler="AppealBlock" class="space-y-6" id="appealForm">
            <input type="hidden" name="jobId" id="appealJobId" />
            <div>
                <label class="block text-[10px] font-black text-slate-400 uppercase tracking-widest mb-2">Giải trình khiếu nại</label>
                <textarea name="message" required id="appealMsg" placeholder="Tại sao bài đăng này hợp lệ?" class="w-full p-4 min-h-[100px] bg-slate-50 border border-slate-100 rounded-xl text-sm font-medium text-slate-900 focus:bg-white focus:border-indigo-400 transition-all outline-none"></textarea>
            </div>
            <div class="flex gap-4">
                <button type="button" class="flex-1 h-12 bg-slate-100 text-slate-700 font-bold rounded-xl hover:bg-slate-200 transition" onclick="closeAppeal()">Hủy</button>
                <button type="submit" id="submitAppealBtn" class="flex-1 h-12 bg-indigo-600 text-white font-bold rounded-xl hover:bg-indigo-700 transition">Gửi</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        function openAppeal(jobId) {
            document.getElementById('appealJobId').value = jobId;
            document.getElementById('appealMsg').value = '';
            document.getElementById('appealModal').classList.remove('hidden');
        }

        function closeAppeal() {
            document.getElementById('appealModal').classList.add('hidden');
        }

        document.getElementById('appealForm').onsubmit = function() {
            var btn = document.getElementById('submitAppealBtn');
            btn.innerHTML = '<i class="fas fa-circle-notch fa-spin mr-2"></i> Đang gửi...';
            btn.disabled = true;
            btn.classList.add('opacity-75', 'cursor-not-allowed');
            return true;
        };
    </script>
}
